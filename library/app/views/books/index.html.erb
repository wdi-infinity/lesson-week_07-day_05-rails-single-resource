<h1> hello </h1>
<%=
# if we want to wirte Ruby style 
# we should add it between 
  %> 
   <p> <%= link_to 'Create new Book' , new_book_path %></p>

<% @books.each do |book| %>
   <h2> <%= book.title %> </h2>
   <p> <%= link_to 'View Book' , book_path(book) %></p>
   <p> <%= link_to 'Edit Book' , edit_book_path(book) %></p>
   <p> <%= link_to 'Delete Book' , book_path(book), 
            method: :delete ,
            data: {confirm: 'Are you sure you want to delete ?'} %></p>

   <% end %>