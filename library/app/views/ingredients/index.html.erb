<h1>Ingredients</h1>
<p> <%=link_to 'Create new ingredient', new_ingredient_path %></p>
<% @ingredients.each do |ingredient| %>

    <h2><%= ingredient.name %></h2>
    <p> <%= link_to 'View' , ingredient_path(ingredient) %></p>
end